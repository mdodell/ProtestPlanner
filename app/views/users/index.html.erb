
<%= provide(:title, 'my events') %>
<%= render 'shared/navbar_dark' %>


<h1>My Events</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% current_user.events.each do |event| %>
      <tr>
        <td><%=event.name %></td>
        <td><p>map </p></td>
        <td><p>event page </p></td>
      </tr>
    <% end %>
  </tbody>


</table>

<h1>Events that I am an organizer for</h1>
<% UserEventRelationship.all do |event| %>
  <% if event.role_type_id == 0 %>
    <tr>
      <td><%=event.name %></td>
      <td><p>map </p></td>
      <td><p>event page </p></td>
    </tr>
  <% end %>
<% end %>

<br>


